version: 2

builds:
  - dir: ./shoutrrr
    main: .
    binary: shoutrrr
    goos:
      - linux
      - windows
    goarch:
      - amd64
      - 386
      - arm
      - arm64
archives:
  - name_template: '{{.ProjectName}}_{{.Os}}_{{.Arch}}'
    formats:
      - tar.gz
      - zip